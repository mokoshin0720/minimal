# setup:
#   config:
#     AWS: <%= ENV["AWS_ACCESS_KEY_ID"] %>
#     AWS2: ${AWS_SECRET_ACCESS_KEY}

build:
  docker:
    web: Dockerfile.prod
  config:
    RAILS_ENV: development
    test: $test

run:
  web: gunicorn minimalist.wsgi