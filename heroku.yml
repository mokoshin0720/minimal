setup:
  config:
    DJANGO_ENV: perfect

build:
  docker:
    web: Dockerfile.prod
  config:
    RAILS_ENV: development
    REPO: 'www.example.org'
    AWS: <%= ENV["AWS_ACCESS_KEY_ID"] %>

run:
  web: gunicorn minimalist.wsgi